# Cross-Module Scope Dependency Checklist: Identity & Access

**Purpose**: Formal pre-`/speckit-tasks` gate on the quality of the requirements created by this spec's newest clarification — that Manager-scope enforcement (FR-004/014/019) now depends on the Organization module's (spec 003) live API, failing closed on failure. This dependency also inverts the tracker's original build order, so it is reviewed at gate rigor rather than as a quick self-check.
**Created**: 2026-09-21
**Feature**: [spec.md](../spec.md) (plan: [plan.md](../plan.md), research: [research.md](../research.md))

**Note**: This custom checklist is generated by the `/speckit-checklist` command based on feature context and requirements.
**Review Ownership**: This checklist is a reviewer-owned requirements-quality review artifact. Mark an item `[x]` only when the reviewer determines the requirements-quality criterion is satisfied.
**Marker Semantics**: `[x]` means the criterion has been reviewed and satisfied for requirements quality. It does not mean implementation work is complete.

## Requirement Completeness

- [x] CHK001 Is there a requirement defining any time bound for "unreachable or timed out," or is that threshold left entirely unspecified? [Gap, Spec §FR-019] — Resolved 2026-09-21: FR-019 now specifies a 2-second bound, reused in the Edge Cases bullet.
- [x] CHK002 Does the spec state whether FR-019's fail-closed rule applies only to Manager-role requests, or also to Director/Admin/Accounts Officer requests that don't rely on the same per-manager scope check? [Gap, Spec §FR-019] — Resolved 2026-09-21: FR-019 now states it applies only to Manager-role requests, since org-wide roles carry no per-manager scope check.
- [x] CHK003 Is there a requirement or documented assumption for what happens when a scope check races a concurrent reassignment happening in Organization at the same moment? [Gap, Coverage] — Resolved 2026-09-21: added as an Assumptions bullet, relying on Organization's own at-most-one-active-assignment guarantee (spec 003 FR-003).
- [x] CHK004 Is "roles that see the whole organization skip the Organization dependency entirely" itself a stated rule, or only inferable from FR-003/FR-006's org-wide-access language? [Gap, Spec §FR-003, FR-006] — Resolved 2026-09-21: same FR-019 edit as CHK002 states this explicitly.

## Requirement Clarity

- [x] CHK005 Is "Manager-scoped request" in FR-019 defined precisely enough to know which endpoints — this module's own, or every other module's — the rule actually covers? [Ambiguity, Spec §FR-019] — Resolved 2026-09-21: FR-004 now states the rule covers any module's endpoint operating on a Teacher or School record.
- [x] CHK006 Does the Assumptions section's "read it live... on each request that needs a scope check" specify whether that means literally every request or allows any caching window, given SC-006 requires immediate reflection but states no caching rule of its own? [Clarity, Spec §Assumptions, SC-006] — Resolved 2026-09-21: new Assumptions bullet makes the no-caching rule an explicit, durable constraint (see CHK016).

## Requirement Consistency

- [x] CHK007 Do the Assumptions section and FR-019 describe the failure condition in matching terms ("unreachable or timed out" vs. "cannot be completed"), or could a reader treat them as two different conditions? [Consistency, Spec §Assumptions vs FR-019] — Resolved 2026-09-21: side effect of the CHK001 fix; FR-019, its Edge Cases bullet, and User Story 3's new acceptance scenario 4 all use matching language now.
- [x] CHK008 Is the Edge Cases bullet on Organization being unreachable scoped identically to FR-019 (both say "Manager-scoped request"), with no broader or narrower implied case in either? [Consistency, Spec §Edge Cases vs FR-019] — Resolved 2026-09-21: same fix as CHK007.

## Acceptance Criteria Quality

- [x] CHK009 Is there a Success Criterion that measures the fail-closed behavior itself, or do SC-003/SC-006 only measure the path where Organization is reachable? [Gap, Spec §Success Criteria] — Resolved 2026-09-21: added SC-007.
- [x] CHK010 Can SC-006's "100% of the time, no re-login required" be objectively verified without the spec also stating whether any caching of Organization's answer is permitted? [Measurability, Spec §SC-006] — Resolved 2026-09-21: the new never-cache Assumptions bullet (CHK016) removes the ambiguity SC-006 depended on.

## Scenario Coverage

- [x] CHK011 Is there an acceptance scenario exercising the Organization-unreachable path, distinct from its description in Edge Cases prose? [Coverage, Gap] — Resolved 2026-09-21: added User Story 3 acceptance scenario 4.
- [x] CHK012 Is there a requirement or scenario describing what a Manager sees during the window between an Organization outage starting and being detected — a generic denial indistinguishable from a real scope violation, or something else? [Coverage, Gap] — Resolved 2026-09-21: added FR-020.

## Edge Case Coverage

- [x] CHK013 Does Edge Case 2 (assignment changes mid-edit) still hold as written now that scope is read live rather than from a local copy, or does it need restating in terms of the live-query model established later in the same Clarifications session? [Consistency, Spec §Edge Cases] — Resolved 2026-09-21: bullet now explicitly ties its answer to the live-query model.
- [x] CHK014 Does the spec distinguish "Organization is unreachable" from "Organization responds but the School/Teacher identifier is malformed or unrecognized," or are both implicitly treated as the same fail-closed case? [Gap, Ambiguity] — Resolved 2026-09-21: added a new Edge Cases bullet distinguishing the two.

## Non-Functional Requirements

- [x] CHK015 Is there any latency budget for the live scope-check call itself relative to SC-001's overall login-time target, or is the dependency's performance cost entirely unconstrained? [Gap, Spec §SC-001] — Resolved 2026-09-21: FR-019 now states the check happens after login, not during it, and so doesn't count against SC-001.
- [x] CHK016 Does the spec state, as a durable rule, that this module must never hold its own cached/duplicate copy of Organization's data — or is that only true today by absence, with nothing preventing a future amendment from quietly reintroducing one? [Gap, Traceability] — Resolved 2026-09-21: added as its own Assumptions bullet.

## Dependencies & Assumptions

- [x] CHK017 Is the build-order implication of this dependency (Identity now depends on Organization, inverting the tracker's original sequencing) stated anywhere in spec.md itself, or only in plan.md's Complexity Tracking where a reader of the spec alone would miss it? [Traceability, Gap] — Resolved 2026-09-21: added as its own Assumptions bullet in spec.md.
- [x] CHK018 Does spec.md's Assumptions section acknowledge that Organization (spec 003) is itself only specified and planned, not implemented, as a risk to this module's own readiness — or does the spec read as if Organization already exists and works? [Gap, Spec §Assumptions] — Resolved 2026-09-21: same bullet as CHK017 states this directly.

## Ambiguities & Conflicts

- [x] CHK019 Does FR-014's "immediately" and FR-019's fail-closed rule give a clear, non-conflicting answer for the specific case where an assignment change is itself what makes Organization briefly unreachable (e.g., mid-migration)? [Conflict, Spec §FR-014 vs FR-019] — Resolved 2026-09-21: added an Assumptions bullet stating the two rules govern non-overlapping situations.
- [x] CHK020 Is Identity's dependency on Organization's API pinned to any notion of a stable contract/version, or is future drift in Organization's API shape left entirely unaddressed? [Gap, Ambiguity] — Resolved 2026-09-21: added an Assumptions bullet explaining compile-time contract enforcement makes a separate versioning scheme unnecessary within the same monolith.

## Notes

- Mark items `[x]` only after review confirms the requirement-quality criterion is satisfied
- Leave items unchecked when they still require clarification, correction, or reviewer evaluation
- `/speckit-implement` reads checklist checkbox state as a gate and must not modify markers
- `checklists/requirements.md` has a separate built-in lifecycle maintained by `/speckit-specify` and `/speckit-clarify` — this file is independent of it
- Add comments or findings inline
- Items are numbered sequentially for easy reference
