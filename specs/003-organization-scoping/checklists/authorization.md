# Authorization & Scoping Checklist: Organization — Manager/School/Teacher Accountability

**Purpose**: Formal pre-`/speckit-tasks` gate on the quality of this spec's authorization/scoping requirements — who may write, read, and see what, and how consistently that is stated. This is the first real domain module and sets the pattern later modules will follow, so it is reviewed at gate rigor rather than as a quick self-check.
**Created**: 2026-09-21
**Feature**: [spec.md](../spec.md) (plan: [plan.md](../plan.md), research: [research.md](../research.md))

**Note**: This custom checklist is generated by the `/speckit-checklist` command based on feature context and requirements.
**Review Ownership**: This checklist is a reviewer-owned requirements-quality review artifact. Mark an item `[x]` only when the reviewer determines the requirements-quality criterion is satisfied.
**Marker Semantics**: `[x]` means the criterion has been reviewed and satisfied for requirements quality. It does not mean implementation work is complete.

## Requirement Completeness

- [x] CHK001 Are the exact roles permitted to write assignments (Director, Admin) enumerated consistently across every write-capable requirement, including the "end without replacement" behavior implied by Edge Case 1 but not itself named in an FR? [Completeness, Spec §Clarifications, FR-001/002/004]
- [x] CHK002 Are read-access roles specified for the accountable-manager query and the portfolio query, given FR-009 names "Director or Admin" for the unassigned-list read but FR-007 and FR-008 name no actor at all? [Gap, Spec §FR-007, FR-008, FR-009]
- [x] CHK003 Is there a requirement stating whether a Manager may view their own current portfolio (self-service read), or is that explicitly declared out of scope rather than left silent? [Gap, Spec §Assumptions]
- [x] CHK004 Does the spec define the expected outcome when the caller's role cannot be determined at all, as distinct from a caller whose role is known but insufficient? [Gap, Coverage]

## Requirement Clarity

- [x] CHK005 Is "Director or Admin" in the write-capable requirements defined unambiguously as either role independently sufficing, with no reading under which both would be required? [Clarity, Spec §FR-001]
- [x] CHK006 Is the distinction between "who performed the action" (FR-006, audit trail) and "who is authorized to perform it" (FR-001/002/004, authorization) kept clear enough that a reader could not conflate the two requirements? [Clarity, Spec §FR-006]

## Requirement Consistency

- [x] CHK007 Are actor names used consistently across each User Story's narrative text, its Acceptance Scenarios, and the corresponding FRs — e.g., does User Story 3's narrative ("Director or Admin views...") match its own Acceptance Scenario 1 ("the Director requests...")? [Consistency, Spec §User Story 3]
- [x] CHK008 Is the "Director and Admin can both write" decision from the Clarifications session reflected identically everywhere a write actor is named, with no leftover "Director-only" phrasing from before that clarification? [Consistency, Spec §Clarifications]

## Acceptance Criteria Quality

- [x] CHK009 Do any Success Criteria measure the authorization boundary itself — that a non-Director/Admin caller's write attempt is rejected — or do SC-001 through SC-005 only measure the authorized path? [Gap, Spec §Success Criteria]
- [x] CHK010 Is SC-001's "no re-login required" claim measurable on its own terms, or does it implicitly assume a session/authentication model the spec never actually specifies? [Measurability, Spec §SC-001]

## Scenario Coverage

- [x] CHK011 Is there an acceptance scenario exercising an unauthorized actor (e.g., a Manager or Teacher) attempting to assign or reassign, and the expected outcome? [Coverage, Gap]
- [x] CHK012 Is there an acceptance scenario establishing who is authorized to end an assignment with no replacement, given Edge Case 1 describes the resulting state but names no actor for the action itself? [Coverage, Spec §Edge Cases]

## Edge Case Coverage

- [x] CHK013 Edge Case 4 requires listing every School/Teacher still pointing at a deactivated Manager — does the spec state who is authorized to see that list, or does it only inherit FR-009's actor scope by unstated implication? [Ambiguity, Spec §Edge Cases]
- [x] CHK014 Does the spec address whether a deactivated Manager's past (non-current) assignment rows remain visible, to the same authorized actors as before, once that Manager is deactivated — or could authorization change retroactively without a stated rule? [Gap]

## Non-Functional Requirements

- [x] CHK015 Does the spec or plan commit to a testable definition of "role-scoped" for this module that a reviewer can verify independently of Identity & Access's implementation status? [Clarity, Plan §Constitution Check Principle VIII]
- [x] CHK016 Is the interim authorization behavior (a real role check against a not-yet-real identity source) itself visible as a stated constraint/assumption at the spec level, or does that decision exist only in plan.md/research.md without surfacing to a spec reader? [Traceability, Gap]

## Dependencies & Assumptions

- [x] CHK017 Is this module's dependency on Identity & Access (spec 002) for role/user resolution stated as a testable assumption, with a clear trigger for when the interim behavior must be replaced? [Spec §Assumptions]
- [x] CHK018 Does the assumption that "Manager and Director identities... are owned by the Identity & Access module" also cover where the Admin role's definition and membership come from, or is Admin left undefined relative to that module's role model? [Gap, Spec §Assumptions]

## Ambiguities & Conflicts

- [x] CHK019 FR-008 (portfolio query) names no actor while FR-009 (unassigned list) names "Director or Admin" — is there any requirement restricting a Manager from viewing another Manager's portfolio, or is that omission intentional and should it be stated as such? [Ambiguity, Spec §FR-008 vs FR-009]
- [x] CHK020 Does FR-011's conflict-rejection requirement account for concurrent writes from two different authorized roles (e.g., a Director and an Admin reassigning the same item at once), or does its "since the requester last read its current state" phrasing implicitly assume same-role contention only? [Conflict, Spec §FR-011]

## Notes

- Mark items `[x]` only after review confirms the requirement-quality criterion is satisfied
- Leave items unchecked when they still require clarification, correction, or reviewer evaluation
- `/speckit-implement` reads checklist checkbox state as a gate and must not modify markers
- `checklists/requirements.md` has a separate built-in lifecycle maintained by `/speckit-specify` and `/speckit-clarify` — this file is independent of it
- Add comments or findings inline
- Items are numbered sequentially for easy reference
