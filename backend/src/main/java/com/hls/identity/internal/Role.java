package com.hls.identity.internal;

/** The five roles FR-002 defines. A User may hold more than one simultaneously. */
public enum Role {
    DIRECTOR,
    MANAGER,
    ADMIN,
    ACCOUNTS_OFFICER,
    TEACHER
}
